## 一、概念
MCP（Model Co⁠ntext Protocol，模型上下文协议）是‌一种开放标准，目的是增强 AI 与外部系统的交互能力。MCP 为 AI 提供了与外部工具、资源和‎服务交互的标准化方式，让 AI 能够访问最新数据‌、执行复杂操作，并与现有系统集成。

+ **轻松增强 AI 的能力**
+ **统一标准，降低使用和理解成本**
+ **打造服务生态，造福广大开发者**

### 核心概念
<!-- 这是一张图片，ocr 内容为：1 BOOUISES:江及于调整系统向普户项目法名冲改造,比加文本,文件,双件,双门泊底等,客户均可以双元1公司接道用这些数码.使用过程.使用过程 够访问最新信息和外部知识,为模型提供更丰富的上下文. :BROMS示范区:服务湖可可以定文可仅有限公司保证和工作系,供各户项和用产直接使用.它85作用定标准化器风防A UI元素(如斜杠命令,快捷操作)呈现给用户,从而简化用户与LLM的交互过程. 3. TOK工具:MON中最双用自行通系统可以提供合普户籍可项用的通放,使A楼型形影放行计算,查询后感或者和分级系统交互,极大打原工 AI的能力范围. -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771321297044-b916b497-1866-40c5-99c3-09d90974396d.png)

### 程序接入 MCP 服务
#### 引入依赖
```xml

  org.springframework.ai
  spring-ai-mcp-client-spring-boot-starter
  1.0.0-M6

```

#### resource 目录新建 json 配置文件
```json
{
  "mcpServers": {
    "amap-maps": {
      "command": "npx.cmd",  // windows系统添加.cmd
      "args": [
        "-y",
        "@amap/amap-maps-mcp-server"
      ],
      "env": {
        "AMAP_MAPS_API_KEY": "改成你的 API Key"
      }
    }
  }
}
```

#### 配置文件
```yaml
spring:
    ai:
      mcp:
        client:
          stdio:
            servers-configuration: classpath:mcp-servers.json
```

#### 工具类中直接调用相应服务，进行依赖注入
```java
// AI调用MCP服务
@Resource
private ToolCallbackProvider toolCallbackProvider;
```

#### chat 会话中调用 MCP 工具服务
```java
@Resource
private ToolCallbackProvider toolCallbackProvider;
/**
     * AI 恋爱报告功能（调用MCP服务）
     */

public String doChatWithMcp(String message, String chatId){
    ChatResponse chatResponse = chatClient
    .prompt()
    .user(message)
    .advisors(spec -> spec.param(CHAT_MEMORY_CONVERSATION_ID_KEY, chatId)
              .param(CHAT_MEMORY_RETRIEVE_SIZE_KEY, 10))
    // 开启日志
    .advisors(new MyLoggerAdvisor())
    .tools(toolCallbackProvider)
    .call()
    .chatResponse();
    String content = chatResponse.getResult().getOutput().getText();
    log.info("content: {}", content);
    return content;
}
```

---

---
## 二、开发 MCP 服务（默认 stdio 本地模式，SSE 供外部调用）
### 服务端
#### 引入依赖
```xml

  org.springframework.ai
  spring-ai-mcp-server-webmvc-spring-boot-starter
  1.0.0-M6

```

#### 配置依赖文件
```yaml
spring:
  application:
    name: image-search-mcp-server
  profiles:
    active: stdio # 可以改为SSE，灵活指定哪套配置
server:
  port: 8127

```

```yaml
spring:
  ai:
    mcp:
      server:
        name: yu-image-search-mcp-server
        version: 0.0.1
        type: SYNC
        # stdio 本地模式
        stdio: true
  # stdio 本地模式需关闭bendiweb服务
  main:
    web-application-type: none
    banner-mode: off
```

```yaml
spring:
  ai:
    mcp:
      server:
        name: yu-image-search-mcp-server
        version: 0.0.1
        type: SYNC

        stdio: false
```

#### 图片搜索实现类（调用 API）
```java
@Service
public class ImageSearchTool {

    // 替换为你的Pexels API 密钥（从官网申请）
    private static final String API_KEY = "你的 API Key";
    // Pexels 常规搜索接口
    private static final String API_URL = "https://api.pexels.com/v1/search";

    @Tool(description = "search image from web")
    public String searchImage(@ToolParam(description = "Search query keyword") String query) {
        try {
            return String.join(",", searchMediumImages(query));
        } catch (Exception e) {
            return "Error search image: " + e.getMessage();
        }
    }

    /**
     * 搜索中等尺寸图片
     * @param query
     * @return
     */
    public List searchMediumImages(String query) {

        Map headers = new HashMap<>();
        headers.put("Authorization", API_KEY);


        Map params = new HashMap<>();
        params.put("query", query);


        String response = HttpUtil.createGet(API_URL)
        .addHeaders(headers)
        .form(params)
        .execute()
        .body();


        return JSONUtil.parseObj(response)
        .getJSONArray("photos")
        .stream()
        .map(photoObj -> (JSONObject) photoObj)
        .map(photoObj -> photoObj.getJSONObject("src"))
        .map(photo -> photo.getStr("medium"))
        .filter(StrUtil::isNotBlank)
        .collect(Collectors.toList());
    }
}
```

#### 测试类
```java
@SpringBootTest
class ImageSearchToolTest {

    @Resource
    private ImageSearchTool imageSearchTool;

    @Test
    void searchImage() {
        String result = imageSearchTool.searchImage("computer");
        Assertions.assertNotNull(result);
    }
}
```

#### 启动类中注册工具
```java
@SpringBootApplication
public class imageSearchMcpServerApplication {

    public static void main(String[] args) {
        SpringApplication.run(YuImageSearchMcpServerApplication.class, args);
    }

    @Bean
    public ToolCallbackProvider imageSearchTools(ImageSearchTool imageSearchTool) {
        return MethodToolCallbackProvider.builder()
        .toolObjects(imageSearchTool)
        .build();
    }
}
```

#### 服务打包，用于启动
使用 Maven‌ Package 命令打包，生成 jar 包

###  客户端
#### json 配置 MCP 服务
```json
{
  "mcpServers": {
    "image-search-mcp-server": {
      "command": "java",
      "args": [
        "-Dspring.ai.mcp.server.stdio=true",
        "-Dspring.main.web-application-type=none",
        "-Dlogging.pattern.console=",
        "-jar",
        "image-search-mcp-server/target/image-search-mcp-server-0.0.1-SNAPSHOT.jar"
      ],
      "env": {}
    }
  }
}
```

#### 接下来参考 一 中的 2，接入 MCP 服务，直接使用依赖注入即可
<!-- 这是一张图片，ocr 内容为：B.程序接入MCP服务 I,引入依赖 II. RESOURCE 目录新建JSON配置... II.配置文件 IV,工具类中直接调用相应服务,.. VCHAT会话中调用MCP工具服务 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771404573729-36aef81d-0b96-40a3-92ab-26fae985e022.png)

### SSE 模式，供外部调用
```yaml
spring:
  application:
    name: yu-image-search-mcp-server
  profiles:
    active: sse #服务端配置文件改为激活SSE模式
server:
  port: 8127
```

```yaml
spring:
  application:
    name: image-search-mcp-server
  profiles:
    active: sse #客户端修改激活方式为SSE
server:
  port: 8127
```

```yaml
spring:
  ai:
    mcp:
      client:
        sse:
          connections:
            server1:
              url: http://localhost:8127
        # 注释，方式启动stdio冲突
        # stdio:
        #   servers-configuration: classpath:mcp-servers.json
```



