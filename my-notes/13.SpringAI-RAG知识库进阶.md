:::color4
**<font style="color:#DF2A3F;">在使用@Resource 依赖注入的时，声明的 bean 一定要是（配置类）的类名：</font>**

**<font style="color:#DF2A3F;">@Resource</font>**

**<font style="color:#DF2A3F;">VectorStore</font>****<font style="color:#DF2A3F;background-color:#FBDE28;"> pgVectorVectorStore</font>****<font style="color:#DF2A3F;">;</font>**

**<font style="color:#DF2A3F;"></font>**

**<font style="color:#DF2A3F;">public</font>****<font style="color:#DF2A3F;background-color:#FBDE28;"> class PgVectorVectorStoreConfig </font>****<font style="color:#DF2A3F;">{</font>**

**<font style="color:#DF2A3F;">public VectorStore </font>****<font style="color:#DF2A3F;background-color:#FBDE28;">pgVectorVectorStore</font>****<font style="color:#DF2A3F;">(JdbcTemplate jdbcTemplate, EmbeddingModel dashscopeEmbeddingModel) </font>**

**}**

****

**命名一定要规范**

:::

## 一、RAG 核心特性
### 文档收集和切割 - ETL
文档收集和切割阶段，我们要对自己准备好的知识库文档进行处理，然后保存到向量数据库中。这个过程俗称 ETL（抽取、转换、加载），Spring AI 提供了对 ETL 的支持，参考 官方文档。

#### ETL
在 Spr⁠ing AI 中，‌对 Document 的处理通常遵循‎以下流程：

读取文档：使用 DocumentReader 组件从数据源（如本地文件、网络资源、数据库等）加载文档。

转换文档：根据需求将文档转换为适合后续处理的格式，比如去除冗余信息、分词、词性标注等，可以使用 DocumentTransformer 组件实现。

写入文档：使用 DocumentWriter 将文档以特定格式保存到存储中，比如将文档以嵌入向量的形式写入到向量数据库，或者以键值对字符串的形式保存到 Redis 等 KV 存储中。

<!-- 这是一张图片，ocr 内容为：DOCUMENT WRITER DOCUMENT TRANSFORMER DOCUMENT READER 文档转换器 文档写入器 文档读取器 文档 文档 DOCUMENT DOCUMENT FUNCTION SUPPLIER CONSUMER 存储 文档 文档 源文件 <LIST<DOCUMENT), <LIST<DOCUMENT>> <LIST<DOCUMENT> STORE LIST<DOCUMENT>> 文档 文档 编程导航 COCLEFARTHER.CN -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770717334513-fb10b6e3-503e-4cc7-9949-738f3cbd3f2d.png)

#### 抽取
Sprin⁠g AI 通过 D‌ocumentReader 组件实现‎文档抽取，也就是把‌文档加载到内存中。

DocumentReader 接口实现了 Supplier<List<Document>> 接口，主要负责从各种数据源读取数据并转换为 Document 对象集合。

```java
public interface DocumentReader extends Supplier<List<Document>> {
    default List<Document> read() {
        return get();
    }
}
```

#### 转换
Sprin⁠g AI 通过 D‌ocumentTransformer‎ 组件实现文档转换‌。

```java
public interface DocumentTransformer extends Function<List<Document>, List<Document>> {
    default List<Document> transform(List<Document> documents) {
        return apply(documents);
    }
}
```

#### 加载
Sprin⁠g AI 通过 D‌ocumentWriter 组件实现‎文档加载（写入）。

```java
public interface DocumentWriter extends Consumer<List<Document>> {
    default void write(List<Document> documents) {
        accept(documents);
    }
}
```

### 向量转换和存储
#### 向量存储的工作原理
在向量数据库⁠中，查询与传统关系型数据‌库有所不同。向量库执行的是相似性搜索，而非精确匹‎配。

:::color4
1. 嵌入转换：当文档被添加到向量存储时，Spring AI 会使用嵌入模型（如 OpenAI 的 text-embedding-ada-002）将文本转换为向量
2. 相似度计算：查询时，查询文本同样被转换为向量，然后系统计算此向量与存储中所有向量的相似度。
3. 相似度度量：常用的相似度计算方法包括：
    1. 余弦相似度：计算两个向量的夹角余弦值，范围在 - 1 到 1 之间
    2. 欧氏距离：计算两个向量间的直线距离
    3. 点积：两个向量的点积值
4. 过滤与排序：根据相似度阈值过滤结果，并按相似度排序返回最相关的文档

:::

:::color4
**<font style="color:#DF2A3F;">开发方式：先准备好数据源 => 引入不同的整合包 ‎=> 编写对应的配置 => 使用自动注入‌的 VectorStore 即可</font>**

:::

#### 基于 PGVector 实现向量存储（自定义）
##### 引入依赖
```xml
<!--    手动整合PGVector向量存储    -->
<dependency>
	<groupId>org.springframework.boot</groupId>
	<artifactId>spring-boot-starter-jdbc</artifactId>
</dependency>
<dependency>
	<groupId>org.postgresql</groupId>
	<artifactId>postgresql</artifactId>
	<scope>runtime</scope>
</dependency>
<dependency>
	<groupId>org.springframework.ai</groupId>
	<artifactId>spring-ai-pgvector-store</artifactId>
	<version>1.0.0-M7</version>
</dependency>
<!--    自动整合PGVector向量存储    -->
<!--        <dependency>-->
<!--            <groupId>org.springframework.ai</groupId>-->
<!--            <artifactId>spring-ai-starter-vector-store-pgvector</artifactId>-->
<!--            <version>1.0.0-M7</version>-->
<!--        </dependency>-->
```

##### 配置数据库连接
```xml
spring:
  datasource:
    url: jdbc:postgresql://rm-cn-fsw4nfruz0001ybo.rwlb.rds.aliyuncs.com:5432/yu_ai_agent
    username: my_user
    password: 110110Asd
  ai:
    vectorstore:
      pgvector:
        index-type: HNSW
        dimensions: 1536
        distance-type: COSINE_DISTANCE
        max-document-batch-size: 10000
```

##### ❤编写配置类
:::color4
在使用EmbeddingModel 时，必须指定对应大模型的 bean，就是声明时候的 bean 名称； JdbcTemplate 同理

EmbeddingModel **dashscopeEmbeddingModel**

:::

<!-- 这是一张图片，ocr 内容为：@CONFIGURATION 解释|添加注释X PUBLIC CLASS PGVECTORSTORECONFIG { 12 少 13 14 @RESOURCE PRIVATE LOVEAPPDOCUMENTLOADER LOVEAPPDOCUMENTLOADER; 15 16 ABEAN 17 解释|添加注释|代码提效IX ] -NUHIS  V-ATORSTORE PQVECTORSTORE(JDBCTEMPLATE JDBCTEMPLATE, EMBEDDINGHODEL DASHSCOPEEMBEDDINGHODEL) 18 JDBCTEMPLATE TORSTORE.BUILDER(JDBCTEMPLATE,EMBEDDINGMODEL).... 19 DASHSCOPEEMBEDDINGMODEL 20 子 21 22 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770741340884-1b782ab0-c59e-4b59-997c-6a7f648d1c6a.png)

```java
@Configuration
public class PgVectorVectorStore {

	// 文档加载器
	@Resource
	private LoveAppDocumentLoader loveAppDocumentLoader;

	@Bean
	public VectorStore pgVectorVectorStore(JdbcTemplate jdbcTemplate, EmbeddingModel dashscopeEmbeddingModel) {
		PgVectorStore vectorStore = PgVectorStore.builder(jdbcTemplate, dashscopeEmbeddingModel)
		.dimensions(1536)
		.distanceType(COSINE_DISTANCE)
		.indexType(HNSW)
		.initializeSchema(true)
		.schemaName("public")
		.vectorTableName("vector_store")
		.maxDocumentBatchSize(10000)
		.build();
		return vectorStore;
	}
}
```

##### 测试
```java
@SpringBootTest
public class PgVectorVectorStoreConfigTest {

	@Resource
	VectorStore pgVectorVectorStore;

	@Test
	void test() {
		List<Document> documents = List.of(
			new Document("Spring AI rocks!! Spring AI rocks!! Spring AI rocks!! Spring AI rocks!! Spring AI rocks!!", Map.of("meta1", "meta1")),
			new Document("The World is Big and Salvation Lurks Around the Corner"),
			new Document("You walk forward facing the past and you turn back toward the future.", Map.of("meta2", "meta2")));

		pgVectorVectorStore.add(documents);

		List<Document> results = pgVectorVectorStore.similaritySearch(SearchRequest.builder().query("Spring").topK(5).build());
		Assertions.assertNotNull(results);
	}
}

```

##### 避免 PgVector 自动配置冲突
```java
@SpringBootApplication(exclude = PgVectorStoreAutoConfiguration.class)
public class AiAgentApplication {

    public static void main(String[] args) {
        SpringApplication.run(AiAgentApplication.class, args);
    }

}
```

##### 修改 chat 方法中的 advisor，使用 PGVector
```java
@Resource
private VectorStore pgVectorVectorStore;

/**
     * 用 RAG 向量知识库进行对话
     * @param message
     * @param chatId
     * @return
     */
public String doChatWithRag(String message, String chatId){
	ChatResponse chatResponse = chatClient
		.prompt()
		.user(message)
		.advisors(spec -> spec.param(CHAT_MEMORY_CONVERSATION_ID_KEY, chatId)
				  .param(CHAT_MEMORY_RETRIEVE_SIZE_KEY, 10))
		// 应用 RAG 知识库问答
		// .advisors(new QuestionAnswerAdvisor(vectorStore))
		// 应用 RAG 检索增强服务（基于 PGVector 向量存储）
		.advisors(new QuestionAnswerAdvisor(pgVectorVectorStore))
		.call()
		.chatResponse();
	String content = chatResponse.getResult().getOutput().getText();
	log.info("content: {}", content);
	return content;
}
```

### 文档过滤和检索
#### 预检索：优化用户查询
查询转换 - 查询重写

查询转换 - 查询翻译

查询转换 - 查询压缩

查询转换 - 多查询扩展

#### 检索：提高查询相关性
文档搜索

文档合并

#### ❤查询增强和关联
⁠QuestionA‌nswerAdvisor 查询增强 （ .advisors(new QuestionAnswerAdvisor(pgVectorVectorStore)) ）

⁠Retrieval‌AugmentationAdviso‎r 查询增强（检索器，文档过滤）

⁠Contextua‌lQueryAugmenter 空上‎下文处理 （错误机制处理）

---

## 二、RAG 最佳实践和调优
### 文档收集和切割
#### 优化原始文档
#### 文档切片
最佳文档切片策略是 **结合智能分块算法和人工二次校验**。智能分块算法基于分句标识符先划分为段落，再根据语义相关性动态选择切片点，避免固定长度切分导致的语义断裂。在实际应用中，应尽量让文本切片包含完整信息，同时避免包含过多干扰信息。

#### 元数据标注（云平台可直接设定 / 代码实现）
##### 云平台标注
<!-- 这是一张图片，ocr 内容为：新建META信息模版 模型 MCP 应用 知识库/创建知识库 取值方法 字段(英文,_) 是否参与检索 操作 中文描述 是否参与模型回复 知识库信息 大模型 恋爱状态 配置实体描述 LOVE STATUS 数据处理 + 添加字段(1/5) METADATA抽取 配置实体描述 EXCEL文件表头是否拼装 实体描述: 文档切分CHUNK 单身,已婚,恋爱(3种恋爱状态只能选择一个) 智 确定 取消 导入完成 上一步 取消 确定 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770802060565-bf1da328-9180-4159-9852-4692cffbe180.png)

##### 手动标注
```java
 public List<Document> loadMarkdowns() {
        List<Document> allDocuments = new ArrayList<>();
        try {
            // 加载多篇文档, '*' 通配符
            Resource[] resources = resourcePatternResolver.getResources("classpath:document/*.md");
            for (Resource resource : resources) {
                String fileName = resource.getFilename();
                // 获取文档最后几个字符作为标签
                String status = fileName.substring(fileName.length() - 6, fileName.length() - 4);
                // 每篇文档的配置
                MarkdownDocumentReaderConfig config = MarkdownDocumentReaderConfig.builder()
                        .withHorizontalRuleCreateDocument(true)
                        .withIncludeCodeBlock(false)
                        .withIncludeBlockquote(false)
                        .withAdditionalMetadata("filename", fileName)
                        .withAdditionalMetadata("status", status)
                        .build();
                MarkdownDocumentReader reader = new MarkdownDocumentReader(resource, config);
                allDocuments.addAll(reader.get());
            }
        } catch (IOException e) {
            log.error("Markdown 文档加载失败", e);
        }
        return allDocuments;
    }
```

##### 自动标注
```java
/**
 * 基于 AI 的文档元信息增强器（为文档补充元信息）
 */
@Component
public class MyKeywordEnricher {

    @Resource
    private ChatModel dashScopeChatModel;

    public List<Document> enrichDocument(List<Document> documents) {
        KeywordMetadataEnricher keywordMetadataEnricher = new KeywordMetadataEnricher(dashScopeChatModel, 5);
        return keywordMetadataEnricher.apply(documents);
    }

}
```

```java
@Resource
private MyKeywordEnricher myKeywordEnricher;

@Bean
@Primary
public VectorStore loveAppVectorStore(EmbeddingModel embeddingModel) {
	SimpleVectorStore simpleVectorStore = SimpleVectorStore.builder(embeddingModel).build();
	// 加载文档
	List<Document> documentList = loveAppDocumentLoader.loadMarkdowns();
	// 自动补充关键元信息
	List<Document> enrichDocuments= myKeywordEnricher.enrichDocument(documentList);
	simpleVectorStore.add(enrichDocuments);
	return simpleVectorStore;
}
```

### 向量转换和存储
#### 向量存储配置 （代码 / 云平台）
```java
public VectorStore loveAppVectorStore(EmbeddingModel embeddingModel) {
        SimpleVectorStore simpleVectorStore = SimpleVectorStore.builder(embeddingModel).build();
        // 加载文档
        List<Document> documentList = loveAppDocumentLoader.loadMarkdowns();
        // 自动补充关键元信息
        List<Document> enrichDocuments= myKeywordEnricher.enrichDocument(documentList);
        simpleVectorStore.add(enrichDocuments);
        return simpleVectorStore;
    }
```

<!-- 这是一张图片，ocr 内容为：在云平台中,通常提供多种存储选项,比如内置的向量存储或者云数据库: 知识库/创建知识库 知识库信息 选择数据 2 知识库配置 配置模式 推荐配置 自定义 完全开放的离线知识库配置,按照 百炼推荐配置,在效果,推理成 检索需求自由配置,获得推理效... 本,检索时延等方面的最佳实践 多轮对话改写 官方向量 EMBEDDING模型 官方排序 排序配置 RANK模型 相似度阈值 0.20 0.01 向量存储类型 内置 ADB-PG 您还未授权ADB-PG实例,授权之后才能选择.去授权7 下一步 创建空知识库 取消 编理导院 COCLETATHERCN -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770802583007-ffe75c99-cb85-413a-a067-43c15f2b75a1.png)

#### 选择合适的嵌入模型
<!-- 这是一张图片，ocr 内容为：选择合适的嵌入模型 嵌入模型负责将文本转换为向量,其质量直接影响相似皮计算和检索准确性.可以在代码中修改: SIMPLEVECTORSTORE VECTORSTORE - SIMPLEVECTORSTORE.BUILDER(EMBEDDINGMODEL) .BUILD(); 云平台通常提供多种嵌入模型选项: 知识库/创建知识库 知识库信息 选择数据 知识库配置 配置模式 推荐配置 自定义 百炼推荐配置,在效果,推理成 完全开放的离线知识库配置,按照 本,检索时延等方面的最佳实践 检索需求自由配置,获得推理效 多轮对话改写 官方向量 EMBEDDING模型 官方排序 排序配置 RANK模型 相似度阈值 0.20 0.01 向量存储类型 内置 ADB-PG 您还未授权ADB-PG实例,授权之后才能选择.去授权7 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770802616273-4bad6961-a6ed-403c-8184-c1e4ae9a5c81.png)

### 文档过滤和检索
#### 多查询扩展 （不建议使用）
```java
MultiQueryExpander queryExpander = MultiQueryExpander.builder()
    .chatClientBuilder(chatClientBuilder)
    .numberOfQueries(3)
    .build();
List<Query> queries = queryExpander.expand(new Query("谁是程序员鱼皮啊？"));
```

```java
DocumentRetriever retriever = VectorStoreDocumentRetriever.builder()
    .vectorStore(vectorStore)
    .similarityThreshold(0.73)
    .topK(5)
    .filterExpression(new FilterExpressionBuilder()
        .eq("genre", "fairytale")
        .build())
    .build();

List<Document> retrievedDocuments = documentRetriever.retrieve(query);

System.out.println(query.text());
```

多查询扩展的完整使用流程可以包括三个步骤：

1. 使用扩展后的查询召回文档：遍历扩展后的查询列表，对每个查询使用 DocumentRetriever 来召回相关文档。
2. 整合召回的文档：将每个查询召回的文档进行整合，形成一个包含所有相关信息的文档集合。（也可以使用 文档合并器 去重）
3. 使用召回的文档改写 Prompt：将整合后的文档内容添加到原始 Prompt 中，为大语言模型提供更丰富的上下文信息。

💡 需要⁠注意，多查询扩展会‌增加查询次数和计算成本，效果也不易量‎化评估，所以个人建‌议慎用这种优化方式。

#### ❤查询重写与翻译
1. **代码：查询重写（对用户的问题进行重写）**

```java
/**
 * 查询重写器
 */
@Component
public class QueryRewriter {

    private final QueryTransformer queryTransformer;

    public QueryRewriter(ChatModel dashScopeChatModel) {
        ChatClient.Builder builder = ChatClient.builder(dashScopeChatModel);
        queryTransformer = RewriteQueryTransformer.builder().chatClientBuilder(builder).build();
    }

    /**
     * 执行查询重写
     * @param prompt
     * @return
     */
    public String doQueryRewrite(String prompt) {
        Query query = new Query(prompt);

        Query transformedQuery = queryTransformer.transform(query);

        return transformedQuery.text();
    }
}
```

```java
// 查询重写器
@Resource
private QueryRewriter queryRewriter;

/**
     * 用 RAG 向量知识库进行对话
     * @param message
     * @param chatId
     * @return
     */
public String doChatWithRag(String message, String chatId){
	// 查询重写
	String rewriteMessage = queryRewriter.doQueryRewrite(message);

	ChatResponse chatResponse = chatClient
	.prompt()
	// 使用改写后的查询
	.user(rewriteMessage)
	.advisors(spec -> spec.param(CHAT_MEMORY_CONVERSATION_ID_KEY, chatId)
			  .param(CHAT_MEMORY_RETRIEVE_SIZE_KEY, 10))
	// 应用 RAG 知识库问答
	// .advisors(new QuestionAnswerAdvisor(vectorStore))
	// 应用 RAG 检索增强服务（基于 PGVector 向量存储）
	.advisors(new QuestionAnswerAdvisor(pgVectorVectorStore))
	.call()
	.chatResponse();
	String content = chatResponse.getResult().getOutput().getText();
	log.info("content: {}", content);
	return content;
}
```

<!-- 这是一张图片，ocr 内容为：EVALUATE EXPRESSION (ENTER) OR ADD A WATCH (CTRL+SHIFT+ENTER) THIS{LOVEAPP@11930] MESSAGE"我已经结婚了,但是婚后关系不太亲密,怎么办?" CHATLD "7A20B76B-85AA-4C3C-847C-771DEA9457E3" REWRITTENMESSAGE"婚后关系不亲密,如何改善? QUERYREWRITERQUERYREWRITER@11934] (CHATCLIENT{DEFAULTCHATCLIENT@11935) OO LOVEAPPVECTORSTORE (SIMPLEVECTORSTORE@11936) -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770805201949-c5481dfd-9752-4e21-8bd2-01ea5b440391.png)

2. **云平台：查询重写**

<!-- 这是一张图片，ocr 内容为：知识库/创建知识库 选择数据 知识库信息 数据类型 田 结构化数据 非结构化数据 选择数据管理功能中已创建的数据 选择数据管理功能中已上传的 DOC,PDF,MD,TXT等文件 表 知识库配置 配置模式 自定义 推荐配置 完全开放的离线知识库配置,按照 百炼推荐配置,在效果,推理成 本,检索时延等方面的最佳实践 检索需求自由配置,获得推理效... 多轮对话改写 官方向量 EMBEDDING模型 官方排序 排序配置 RANK模型( 相似度阈值 0.20 0.01 下一步 创建空知识库 取消 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770805279834-2c0b32ff-c8e9-4e63-bd0b-7ae79463666c.png)

#### ❤检索器（重点：基于元数据过滤）
检索器配置⁠是影响检索质量的关‌键因素，主要包括三个方面：相似度阈值‎、返回文档数量和‌**过滤规则**。

1. 相似度阈值

<!-- 这是一张图片，ocr 内容为：在编程实现中,可以通过文档检索器配置: RER DOCUMENTRETRIEVER - VECTORSTOREDOCUMENTRETRIEVER.BUILDER() DOCUMENTRETRIEVER DOC VECTORSTORE(LOVEAPPVECTORSTORE) .SIMILARITYTHRESHOLD(0.5) .BUILD(); 云平台提供了更便捷的配置界面,参考文档: 知识库配置 配置模式 多轮对话改写 开启 EMBEDDING模型 官方向量 官方向量 EMBEDDING模型 官方排序 排序配置 RANK模型 相似度阈值 0.20 0.01 编理导航 CODEFATHER.CN -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770805636773-67a2106d-a830-4c29-8644-7cb6e1e41e4c.png)

2. 控制返回文档数量（召回片段数）

<!-- 这是一张图片，ocr 内容为：平.在编程实现中,可以通过文档检索器配置: 控制返回给模型的文档数量,平衡信息完整性和噪音水平.在编稻 DOCUMENTRETRIEVER DOCUMENTRETRIEVER 二 VECTORSTORSTOREDOCUMENTRIEVER.BUILDER() .VECTORSTORE(LOVEAPPVECTORSTORE) .SIMILARITYTHRESHOLD(0.5) .TOPK(3) .BUILD(); 使用云平台,可以在编辑百炼应用时调整召回片段数,参考文档的提高召回片段数部分: 检索设置 分享渠道 智能拼装 知识库拼装策略 按召回数量 测试版本 20 召回片段数 20 知识库拼装最大长度 2000 6144 编程导航 -SOCLEFATHER.CN 召回片股级即多路召回带路年的上海.系统阅统会进取细体放成分数总两的K个文本初片,不合适的K面回答导致DAG,预约正在的文本初斤,影月回答. 质量. 在多路自向公司下,如果应用大厦了多个月18年,系统合从工业有效及不切与,这后请于0010年,运出意得天行0(只是成分大院和修部分,如果 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770805667640-4655cfee-5c97-4b45-a096-4c51f3412ecc.png)

3. **<font style="color:#DF2A3F;">❤</font>****<font style="color:#DF2A3F;"> 配置文档过滤规则 （自定义 & 基于 Advisor 拦截器实现元数据过滤）</font>**

**编写配置类**

```java
/**
 * 创建自定义的 RAG 检索增强顾问的工厂
 */
@Component
public class LoveAppRagCustomAdvisorFactory {
	/**
     * 创建一个自定义的 RAG 检索增强顾问
     * @param vectorStore 向量存储
     * @param status 状态
     * @return 自定义的 RAG 检索增强顾问
     */
	public static Advisor createLoveAppRagCustomAdvisorFactory(VectorStore vectorStore, String status){
		// 过滤特定状态的文档，"status" 这个为标签的英文名
		Filter.Expression expression = new FilterExpressionBuilder().eq("status", status).build();
		// 创建文档检索器
        DocumentRetriever documentRetriever = VectorStoreDocumentRetriever.builder()
                .vectorStore(vectorStore)
                .filterExpression(expression) // 过滤条件
                .similarityThreshold(0.5) // 相似度阈值
                .topK(3) // 返回的文档数量
                .build();
		
		return RetrievalAugmentationAdvisor.builder()
		.documentRetriever(documentRetriever)
		// .queryAugmenter()
		.build();
	}

}
```

**修改 chat 中 Advisor 拦截器方法**

```java
@Resource
private PgVectorVectorStore pgVectorVectorStore;

/**
     * 用 RAG 向量知识库进行对话
     * @param message
     * @param chatId
     * @return
     */
public String doChatWithRag(String message, String chatId){
	// 查询重写
	String rewriteMessage = queryRewriter.doQueryRewrite(message);

	ChatResponse chatResponse = chatClient
	.prompt()
	// 使用改写后的查询
	.user(rewriteMessage)
	.advisors(spec -> spec.param(CHAT_MEMORY_CONVERSATION_ID_KEY, chatId)
			  .param(CHAT_MEMORY_RETRIEVE_SIZE_KEY, 10))
	// 应用 RAG 知识库问答
	// .advisors(new QuestionAnswerAdvisor(vectorStore))
	// 应用 RAG 检索增强服务（基于 PGVector 向量存储）
	// .advisors(new QuestionAnswerAdvisor(pgVectorVectorStore))
	.advisors(
		LoveAppRagCustomAdvisorFactory.createLoveAppRagCustomAdvisorFactory
		(pgVectorVectorStore, "已婚")
	)
	.call()
	.chatResponse();
	String content = chatResponse.getResult().getOutput().getText();
	log.info("content: {}", content);
	return content;
}
```

### 查询增强和关联
#### ❤错误机制处理（检索不到相关内容）
在实际应用⁠中，可能出现多种异常‌情况，如找不到相关文档、相似度过低、查询‎超时等。良好的错误处‌理机制可以提升用户体验。



异常处理主要包括：

+ 允许空上下文查询（即处理边界情况）
+ 提供友好的错误提示
+ 引导用户提供必要信息

边界情况处⁠理可以使用 Spri‌ng AI 的 ContextualQu‎eryAugment‌er **上下文查询增强器**：

```java
/**
 * 创建上下文查询增强器的工厂
 */
public class LoveAppContextualQueryAugmenterFactory {
    public static ContextualQueryAugmenter createInstance() {
        PromptTemplate emptyContextPromptTemplate = new PromptTemplate("""
                你应该输出下面的内容：
                抱歉，我只能回答恋爱相关的问题，别的没办法帮到您哦，
                有问题可以联系编程导航客服 https://codefather.cn
                """);
        return ContextualQueryAugmenter.builder()
				// 若为true，则检索不到相关内容时，使用大模型回复
                .allowEmptyContext(false)
				// 如果搜索不到相关文档，将会返回指定的提示词
                .emptyContextPromptTemplate(emptyContextPromptTemplate)
                .build();
    }
}
```

#### 其他建议：
<!-- 这是一张图片，ocr 内容为：除了上述优化策略外,还可以考虑以下方面的改进: 问题类型 改进策略 建议选择合适的大模型,提升语义理解能力 大模型并未理解知识和用户提示词之间的关系,答案生硬拼凑 返回的结果没有按照要求,或者不够全面 建议优化提示词模板,引导模型生成更符合要求的回答 返回结果不够准确,混入了模型自身的通用知识 建议开启拒识功能,限制模型只基于知识库回答 建议调整大模型参数,如温度值等 相似提示词,希望控制回答的一致性或多样性 如果有必要的话,还可以考虑更高级的优化方向,比如: 1.分离检索阶段和生成阶段的知识块 2.针对不同阶段使用不同粒度的文档,进一步提升系统性能和回答质量 3.针对查询重写,关验问元信总将紧等用到AL大楼型的场景,可以选择的大模型,不一定整个项目只引入一种大模型, -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770817883060-e9671117-33f1-43a2-9fd3-141738c17913.png)

---

## 三、扩展知识 - RAG 高级知识
### 混合检索策略
<!-- 这是一张图片，ocr 内容为：主要检索方法比较表: 检索方法 劣势 优势 原理 基于嵌入向量相似度搜索 向量检索 理解语义关联,适合概念性查询 对关键词不敏感,召回可能不准确 基于倒排索引,匹配关键词 全文检索 精确匹配关键词,高召回率 不理解语义,同义词难以匹配 结构化检索 依赖良好的元数据,灵活性有限 精确过滤,支持复杂条件组合 基于元数据或结构化字段查询 发现隐含关系,回答复杂问题 构建成本高,需要专业知识 知识图谱检索 利用实体间关系进行图遍历 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1770885925954-fcdb4fd5-bd06-4aa8-8b9c-d975e0a636d4.png)

### ❤大模型幻觉
:::color4
概念：大模型幻觉指的⁠是模型生成看似合理但实际上‌不准确或完全虚构的内容



`**原因**`：

1. 模型的训练数据中可能包含错误或过时的信息
2. 大语言模型本质上是 预测下一个词的概率 模型，它们倾向于生成流畅而未必准确的内容。更重要的是，模型并不真正 “知道” 什么，它只是学会了文本的统计模式。



`**<font style="color:#DF2A3F;">减少幻觉</font>**`：

1. **使用 RAG 检索增强生成，通过引入‌外部知识源**，我们可以让模型不再完全依赖其参数中存储的‎信息，而是基于检索到的最新‌、准确的信息来回答问题。
+ 有效的 RAG 实现通⁠常会引入 “**引用标注**” 机制，让模型明确指出‌信息**来源于哪个文档的哪个部分**。当模型不确定时，我们也**应该鼓励它诚实地表达不确定性**，而不是猜‎测答案。
2. **提示词工程优化**，可以采用 “‌思维链” 提高推理透明度，通过**引导模型一步步思考**，我们能够更好地观察其推理过程‎，及时发现可能的错误。
3. 使用 **事实验证模型** 检查生成内容的准确性，**建立关键信息的自动核查机制**，或**实施人机协作的审核流程**。评估幻觉程度的指标包括事实一致性、引用准确性和自洽性评分。

:::

