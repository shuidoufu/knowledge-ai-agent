## 一、AI 应用接口开发
### 开发同步调用接口
```java
@Resource
private LoveApp loveApp;

/**
     * 同步调用 AI 恋爱大师应用
     *
     * @param message
     * @param chatId
     * @return
     */
@GetMapping("/love_app/chat/sync")
public String doChatWithLoveAppSync(String message, String chatId) {
    return loveApp.doChat(message, chatId);
}
```

### 开发异步调用接口，添加支持流式调用的 chat
```java
/**
     * AI 基础对话（支持多轮对话记忆，SSE 流式传输）
     *
     * @param message
     * @param chatId
     * @return
     */
public Flux<String> doChatByStream(String message, String chatId) {
    return chatClient
    .prompt()
    .user(message)
    .advisors(spec -> spec.param(ChatMemory.CONVERSATION_ID, chatId))
    .stream()
    .content();
}
```

```java
// 基于注解（简单）
/**
 * SSE 流式调用 AI 恋爱大师应用
 *
 * @param message
 * @param chatId
 * @return
 */
@GetMapping(value = "/love_app/chat/sse", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
public Flux<String> doChatWithLoveAppSSE(String message, String chatId) {
    return loveApp.doChatByStream(message, chatId);
}


// 基于接口（灵活）
/**
 * SSE 流式调用 AI 恋爱大师应用
 *
 * @param message
 * @param chatId
 * @return
 */
@GetMapping(value = "/love_app/chat/sse_emitter")
public SseEmitter doChatWithLoveAppServerSseEmitter(String message, String chatId) {
    // 创建一个超时时间较长的 SseEmitter
    SseEmitter sseEmitter = new SseEmitter(180000L); // 3 分钟超时
    // 获取 Flux 响应式数据流并且直接通过订阅推送给 SseEmitter
    loveApp.doChatByStream(message, chatId)
    .subscribe(chunk -> {
        try {
            sseEmitter.send(chunk);
        } catch (IOException e) {
            sseEmitter.completeWithError(e);
        }
    }, sseEmitter::completeWithError, sseEmitter::complete);
    // 返回
    return sseEmitter;
}
```

---

## 二、AI 智能体接口开发 （Manus）
### 使用 SseEmitter，Base 中添加流式运行方法
**<font style="color:#DF2A3F;">每次使用完，一定要完成关闭 sseEmitter</font>**

```java
/**
 * 运行代理（流式输出）
 *
 * @param userPrompt 用户提示词
 * @return 执行结果
 */
public SseEmitter runStream(String userPrompt) {
// 创建一个超时时间较长的 SseEmitter
SseEmitter sseEmitter = new SseEmitter(300000L); // 5 分钟超时
// 使用线程异步处理，避免阻塞主线程
CompletableFuture.runAsync(() -> {
    // 1、基础校验
    try {
        if (this.state != AgentState.IDLE) {
            sseEmitter.send("错误：无法从状态运行代理：" + this.state);
            sseEmitter.complete();
            return;
        }
        if (StrUtil.isBlank(userPrompt)) {
            sseEmitter.send("错误：不能使用空提示词运行代理");
            sseEmitter.complete();
            return;
        }
    } catch (Exception e) {
        sseEmitter.completeWithError(e);
    }
    // 2、执行，更改状态
    this.state = AgentState.RUNNING;
    // 记录消息上下文
    messageList.add(new UserMessage(userPrompt));
    // 保存结果列表
    List<String> results = new ArrayList<>();
    try {
        // 执行循环
        for (int i = 0; i < maxSteps && state != AgentState.FINISHED; i++) {
            int stepNumber = i + 1;
            currentStep = stepNumber;
            log.info("Executing step {}/{}", stepNumber, maxSteps);
            // 单步执行
            String stepResult = step();
            String result = "Step " + stepNumber + ": " + stepResult;
            results.add(result);
            // 输出当前每一步的结果到 SSE
            sseEmitter.send(result);
        }
        // 检查是否超出步骤限制
        if (currentStep >= maxSteps) {
            state = AgentState.FINISHED;
            results.add("Terminated: Reached max steps (" + maxSteps + ")");
            sseEmitter.send("执行结束：达到最大步骤（" + maxSteps + "）");
        }
        // 正常完成
        sseEmitter.complete();
    } catch (Exception e) {
        state = AgentState.ERROR;
        log.error("error executing agent", e);
        try {
            sseEmitter.send("执行错误：" + e.getMessage());
            sseEmitter.complete();
        } catch (IOException ex) {
            sseEmitter.completeWithError(ex);
        }
    } finally {
        // 3、清理资源
        this.cleanup();
    }
});

// 设置超时回调
sseEmitter.onTimeout(() -> {
    this.state = AgentState.ERROR;
    this.cleanup();
    log.warn("SSE connection timeout");
});
// 设置完成回调
sseEmitter.onCompletion(() -> {
    if (this.state == AgentState.RUNNING) {
        this.state = AgentState.FINISHED;
    }
    this.cleanup();
    log.info("SSE connection completed");
});
return sseEmitter;
}
```

### 开发调用 Manus 接口
```java
/**
     * 流式调用 Manus 超级智能体
     *
     * @param message
     * @return
     */
@GetMapping("/manus/chat")
public SseEmitter doChatWithManus(String message) {
YuManus yuManus = new YuManus(allTools, dashscopeChatModel);
return yuManus.runStream(message);
}
```

### 全局跨域配置（直接使用）
```java
/**
 * 全局跨域配置
 */
@Configuration
public class CorsConfig implements WebMvcConfigurer {

    @Override
    public void addCorsMappings(CorsRegistry registry) {
        // 覆盖所有请求
        registry.addMapping("/**")
        // 允许发送 Cookie
        .allowCredentials(true)
        // 放行哪些域名（必须用 patterns，否则 * 会和 allowCredentials 冲突）
        .allowedOriginPatterns("*")
        .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
        .allowedHeaders("*")
        .exposedHeaders("*");
    }
}
```

---

## 三、AI 服务 Serverless 部署（云部署）
使用 Serverless 平台，开发者只需关注业务代码的编写，无需管理服务器等基‎础设施，系统会根据实际使用量自动扩容并按使用付‌费，从而显著降低运维成本和开发复杂度。

<!-- 这是一张图片，ocr 内容为：SERVERLESS云平台 后端容器 前端容器 JDK 21 NGINX VUE3静态文件 SPRING BOOT应用 (HTML/CSS/JS) DOCKER DOCKER 编程导航 CODEFATHERC -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771848422915-d5c56d43-fa63-43de-b5ec-71cf56cdea94.png)

### 后端部署
#### 编写生产环境的配置文件
```yaml
spring:
  datasource:
    url: jdbc:postgresql://rm-cn-fsw4nfruz0001ybo.rwlb.rds.aliyuncs.com:5432/yu_ai_agent
    username: my_user
    password: 110110Asd
  application:
    name: ai-agent
  # SpringAI引用大模型
  ai:
    dashscope:
      api-key: sk-4204fb2fc4f1423ba5ada4399f36448a
      chat:
        options:
          model: qwen-plus
    vectorstore:
      pgvector:
        index-type: HNSW
        dimensions: 1536
        distance-type: COSINE_DISTANCE
        max-document-batch-size: 10000
#    mcp服务接口
#    mcp:
#      client:
#        stdio:
#          servers-configuration: classpath:mcp-servers.json
#        SSE模式，供本服务给外部使用
# 			 sse:
#   			 connections:
#     				server1:
#      				 url: http://localhost:8127
#  profiles:
#    active: local
server:
  port: 8123
  servlet:
    context-path: /api

# springdoc-openapi ??????
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.example.aiagent.controller # ??????

#knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn
```

#### 构建 Docker 容器镜像
##### 运行时打包（推荐）
**<font style="color:#DF2A3F;">在后端项目中创建 Dockerfile 文件，用于自动化构建 Docker 容器镜像</font>**

```dockerfile
FROM maven:3.9-amazoncorretto-21
WORKDIR /app


COPY pom.xml .
COPY src ./src


RUN mvn clean package -DskipTests


EXPOSE 8123


CMD ["java", "-jar", "/app/target/yu-ai-agent-0.0.1-SNAPSHOT.jar", "--spring.profiles.active=prod"]
```

##### 预打包
先将项目使用 mvn 命令将文件提前打包好，生成 jar 文件，之后复制到 docker 空间中即可

```dockerfile
FROM openjdk:21-slim


WORKDIR /app


COPY target/yu-ai-agent-0.0.1-SNAPSHOT.jar app.jar


EXPOSE 8123


CMD ["java", "-jar", "app.jar", "--spring.profiles.active=prod"]
```

#### 使用平台部署容器
<!-- 这是一张图片，ocr 内容为：通知 帮助 环境BACK-ENVIRONMENT 微信云托管 服务列表 服务管理 模板部署 新建服务 搜索服务名称 服务列表 暂无服务数据,请先新建服务 运行日志 新建服务 使用模板一键部署 操作记录 服务名称 YU-AI-AGENT-BACKEND 云调用 自定义域名 开启后,允许通过公网域名访问测试当前服务. 公网访问 1.公网域名仅能支持测试使用,请勿用于线上生产. BMYSQL 2.公网域名没有防刷防 DDOS 能力,请尽量使用 CALLCONTAINER 方式. 对象存储 开启后,允许通过内网域名访问测试当前服务. 内网访问 资源监控 确定 取消 费用中心 密设置 田拓展功能 编程导航 DEFATHERCR -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771854196102-0ae36cfb-fc91-443e-9e63-51918b878097.png)

<!-- 这是一张图片，ocr 内容为：服务列表/YU-AI-AGENT-BACKEND 部署发布 运行日志 云端调试 服务监控 服务设置 选择代码 部署发布 手动上传代码包 选择方式 压缩包(.ZIP/JAR/.WAR) 文件夹 上传方式 选择文件 归档.ZIP 代码包 端口 8123 常见端口为80,8080,3000等,需与服务监听端口一致. 编程导航 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771854213398-121a3e8c-7dbf-4127-ae33-a8985954c0ca.png)

<!-- 这是一张图片，ocr 内容为：高级设置 环境变量 JSON KEY VALUE 目标目录 需要构建的代码目录,与DOCKERFILE同级. DOCKERFILE O有     有                                                                                              文件 DOCKERFILE DOCKERFILE 名称 目标目录同级的DOCKERFILE文件,需与代码目标目录同级. 备注 发布 编程导航 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771854229900-60603dfd-2c93-4a40-8e7e-182b3841e014.png)

<!-- 这是一张图片，ocr 内容为：服务列表/YU-AI-AGENT-BACKEND 云端调试 服务监控 运行日志 服务设置 部署发布 关闭服务 基础信息 流水线 镜像仓库 公网访问 HTTPS://YU-AI-AGENT-BACKEND-119344-6-1256524210.SH.RUN.TCLOUDBASE.COM )公网域名仅能支持测试使用.请勿用于线上生产,且公网域名没有防刷防DDOS能力. 内网访问 编程号 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771854275483-6c112d8c-c56c-401a-8faf-18c7b2382de6.png)

### 前端部署
#### ❤ 前端生产环境配置（<font style="color:#DF2A3F;">index.js/request.js</font>）
```javascript
const API_BASE_URL = process.env.NODE_ENV === 'production' 
  ? '/api' 
  : 'http://localhost:8123/api'
```

#### 配置 Nginx 反向代理，解决跨域问题
新建 nginx.conf 文件

```xml
server {
    listen       80;
    server_name  localhost;
    
    
    root   /usr/share/nginx/html;

    
    location / {
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    
    location ^~ /api/ {

        # 后端云服务地址
        proxy_pass https://yu-ai-agent-backend-119344-6-1256524210.sh.run.tcloudbase.com/api/;
        
        
        proxy_set_header Host yu-ai-agent-backend-119344-6-1256524210.sh.run.tcloudbase.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        
        proxy_set_header Connection ""; 
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding off;
        proxy_read_timeout 600s;
        
        
        proxy_intercept_errors off;
    }

    
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        access_log off;
        add_header Cache-Control "public";
    }

    
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
```

#### 构建 Docker 容器镜像
1. 编写前端 ⁠Dockerfil‌e 文件，定义了打包构建和 Ngin‎x 配置的流程

```dockerfile
FROM node:20-alpine AS build
WORKDIR /app
COPY . .
RUN npm install
RUN npm run build


FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html

COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

2. 可以创建 .dockerignore 文件忽略不必要的文件，防止 Docker 将 node_modules 等部署时用不到的文件拷贝到工作空间

```dockerfile
 依赖目录
node_modules
npm-debug.log
yarn-debug.log
yarn-error.log

# 编译输出
/dist
/build

# 本地环境文件
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 编辑器目录和配置
/.idea
/.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 操作系统文件
.DS_Store
Thumbs.db

# 测试覆盖率报告
/coverage

# 缓存
.npm
.eslintcache

# 日志
logs
*.log

```

#### 使用平台部署容器
<!-- 这是一张图片，ocr 内容为：服务列表 模板部署 搜索服务名称 新建服务 YU-AI-AGENT-BACKEND 未发布/分配流量 新建服务 使用模板一键部署 上次修改在4分钟前 服务名称 YU-AI-AGENT-FRONTEND 开启后,允许通过公网域名访问测试当前服务. 公网访问 1.公网域名仅能支持测试使用,请勿用于线上生产. 2,公网域名没有防刷防 DDOS 能力,请尽量使用 CALLCONTAINER 方式. 开启后,允许通过内网域名访问测试当前服务. 内网访问 取消 确定 编程导航 CODEFATHER.CN -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771855148492-34444692-9e32-4f0f-82d3-04f9d8a5e9c6.png)

<!-- 这是一张图片，ocr 内容为：服务列表/YU-AI-AGENT-FRONTEND 部署发布 运行日志 服务监控 服务设置 云端调试 部署发布 选择代码 选择方式 手动上传代码包 夹压缩包(.ZIP/.JAR/.WAR) 文件夹 上传方式 选择文件 代码包 归档.ZIP 端口 80 常见端口为80,8080,3000等,需与服务监听端口一致. 高级设置 环境变量 JSON KEY VALUE 目标目录 需要构建的代码目录,与DOCKERFILE同级. DOCKERFILE 有 无 文件 -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771855157365-762361b1-d02a-47f6-9f1d-cacb029a9c9a.png)

<!-- 这是一张图片，ocr 内容为：至此,我们355项目前后请备定定了,经这把交通型本数80本数后相接为口,这样在项目及有D日至60项放会自动通客,流DHD度,不用BBO 记得及时删除掉. 服务列表/YU-AI-AGENT-BACKEND 服务设置 服务监控 运行日志 云端调试 部署发布 关闭服务 流水线 基础信息 镜像仓库 HTTPS://YU-AGENT-BACKEND-119344-6-1256524210.SH.RUN.RUN.TCLOUDBASE.COM 公网访问 公网域名仅能支持测试使用,请勿用于线上生产,且公网域名没有防刷防DDOS 能力. 内网访问 按需调整 容器规格 1核 内存 CPU 2G 最大 实例副本数 最小 5 扩缩容条件 CPU使用率 60% 编程导航 CODEFATHERCN -->
![](https://cdn.nlark.com/yuque/0/2026/png/29575373/1771855198594-d6a3b157-8b22-41e5-93c1-3bac4ba6e667.png)

